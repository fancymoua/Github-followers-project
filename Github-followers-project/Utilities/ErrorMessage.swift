

import UIKit

enum ErrorMessageEnum: String {
    case invalidUsername = "This username is invalid."
    case unableToComplete = "Unable to complete request. Please check internet request."
    case invalidResponse = "Invalid respons from server. Something other than 200."
    case invalidData = "Data is invalid"
}
